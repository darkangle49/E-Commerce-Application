server:
 port: 8082
 
spring:
  application:
    name: CATALOG
      
eureka:
  client:
   register-with-eureka: true
   fetch-registry: true
   service-url:
      defaultZone: 'Eureka Client'
  instance:
    hostname: -      